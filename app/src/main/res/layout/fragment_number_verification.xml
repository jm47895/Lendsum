<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorSecondaryLight"
    tools:context="com.lendsumapp.lendsum.ui.NumberVerificationFragment">

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="This is the Number verification fragment"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/number_verifiction_back_btn"
        style="@style/LoginButtonTheme"
        android:layout_width="0dp"
        android:text="@string/cap_back"
        app:layout_constraintEnd_toStartOf="@+id/number_verifiction_next_btn"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_bias="1"/>

    <Button
        android:id="@+id/number_verifiction_next_btn"
        style="@style/LoginButtonTheme"
        android:layout_width="0dp"
        android:text="@string/next"
        app:layout_constraintBottom_toBottomOf="@+id/number_verifiction_back_btn"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/number_verifiction_back_btn"
        app:layout_constraintTop_toTopOf="@+id/number_verifiction_back_btn" />

</androidx.constraintlayout.widget.ConstraintLayout>